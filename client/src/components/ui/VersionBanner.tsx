/**
 * VERSION BANNER — IMPLEMENT WHEN SITE LAUNCHES
 *
 * A soft-glowing fixed banner at the top of the viewport showing:
 * - Current version (e.g., "Alpha v0.1.0")
 * - Brief status message
 * - Subtle purple glow effect
 * - Dismissible (stores dismissed state in localStorage)
 *
 * Usage: <VersionBanner version="0.1.0" status="alpha" />
 *
 * NOT YET IMPLEMENTED — placeholder for post-launch.
 */
export default function VersionBanner() {
  // TODO: Implement when site launches
  return null;
}
